import { Meta } from '@storybook/addon-docs';

<Meta title="Libs/Build Web/Overview" />

# Build Web Applications

Esta biblioteca contem scripts reutilizavel para projetos React e tambem todas as configurações necessarias para rodar um projeto React com lint, jest, commitlint, prettier, babel e nextjs.

## Como utilizar

```
npm i -D @hubert/build-web
```

Adicione no `package.json`:

```json
{
  "scripts": {
    "prepare": "hubert-web prepare || exit 0",
    "dev": "hubert-web dev",
    "build": "hubert-web build",
    "test": "hubert-web test",
    "test:update": "hubert-web test-update",
    "test:watch": "hubert-web test-watch",
    "test:ci": "hubert-web test-ci",
    "lint": "hubert-web lint",
    "mock": "hubert-cli mock src/mocks"
  }
}
```

Keep calm and code on 🤘.
