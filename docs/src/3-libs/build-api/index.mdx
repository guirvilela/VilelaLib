import { Meta } from '@storybook/addon-docs';

<Meta title="Libs/Build API/Overview" />

# Build API Applications

Esta biblioteca contem scripts reutilizavel para projetos REST API e tambem todas as configurações necessarias para rodar um projeto NodeJS com lint, jest, commitlint, prettier, babel e express.

## Como utilizar

```
npm i -D @hubert/build-api
```

Adicione no `package.json`:

```json
{
  "scripts": {
    "prepare": "hubert-api prepare || exit 0",
    "dev": "hubert-api dev",
    "start": "hubert-api start",
    "test": "hubert-api test",
    "test:update": "hubert-api test-update",
    "test:ci": "hubert-api test-ci",
    "lint": "hubert-api lint",
    "build": "hubert-api build"
  }
}
```

Keep calm and code on 🤘.
