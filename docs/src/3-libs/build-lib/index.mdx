import { Meta } from '@storybook/addon-docs';

<Meta title="Libs/Build Lib/Overview" />

# Build Librarys

Esta biblioteca contem scripts reutilizavel para criação de outras bibliotecas e tambem todas as configurações necessarias para buildar e testar.

## Como utilizar

```
npm i -D @hubert/build-lib
```

Adicione no `package.json`:

```json
{
  "scripts": {
    "prebuild": "hubert-lib prebuild",
    "build": "hubert-lib build",
    "test": "hubert-lib test",
    "test:update": "hubert-lib test-update",
    "test:ci": "hubert-lib test-ci",
    "lint": "hubert-lib lint",
    "prepack": "hubert-lib prepack"
  }
}
```

Keep calm and code on 🤘.
